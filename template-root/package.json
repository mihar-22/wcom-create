{
  "name": "{{LIB_NAME}}",
  "private": true,
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=12"
  },
  "scripts": {
    "build": "wcom build",
    "changelog": "wcom changelog",
    "lint": "npm run lint:prettier && npm run lint:eslint",
    "lint:prettier": "prettier --write --ignore-path .gitignore",
    "lint:eslint": "eslint --quiet --fix --ignore-path .gitignore",
    "release:prepare": "wcom prepare",
    "release:prepare.ci": "wcom prepare --ci",
    "release": "wcom release",
    "validate": "wcom validate"
  },
  "devDependencies": {
    "@wcom/cli": "^1.0.0",
    "eslint": "^7.8.1",
    "eslint-config-airbnb-typescript": "^12.0.0",
    "eslint-config-prettier": "^7.1.0",
    "eslint-plugin-import": "^2.22.0",
    "husky": "^4.3.6",
    "lint-staged": "^10.5.3",
    "prettier": "2.2.1",
    "typescript": "^4.0.2"
  }
}
